# Ferramentas

Registradores (ARISP) Certidão: Download de Recibo expõe 1 ferramenta (todas somente leitura).

### 1. `registradores_certid_recibo_consultar`
**Input**: `email` (opcional), `senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `tipo_login` (opcional), `numero_pedido` (opcional)

Registradores (ARISP) Certidão: Download de Recibo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Registradores (ARISP) Certidão: Download de Recibo
```
